# Required server-side credential. Never expose this value to the browser.
OPENAI_API_KEY=

# Required server-side model identifier used for pitch generation.
OPENAI_MODEL=

# Set to true only for the clearly labeled browser demo fallback.
NEXT_PUBLIC_ENABLE_DEMO_FALLBACK=false
